# 註解慣例（共用）

適用於 `*.ts`、`*.vue`、`*.css` 等程式碼內註解。Markdown 文件不受此限。

## 原則

- **短而重要**：一行能說完就不寫兩行
- **雙語**：英文 + 繁體中文（順序不拘，兩者皆須出現）
- **只說用途**：這段程式碼／區塊**是做什麼的**
- **不寫**：實作經過、歷史、為何這樣改、如何使用、範例列舉（除非使用者明確要求）

## 格式範例

```ts
// Session cache / 練習場次快取

/* Primary action buttons / 主要行動按鈕 */
```

```ts
// ❌ 過長、說明用法
// Call To Action：使用者點擊後會導向練習頁，例如「開始今日練習」

// ✅ 只說用途
// Primary action buttons / 主要行動按鈕
```

## TODO

`TODO` 註解另當別論，可寫待辦內容、條件；仍宜簡短。

```ts
// TODO: Wire API base URL switch / 接上 API base URL 切換
```

## 何時不寫註解

程式碼已自解釋時省略；不為了湊雙語而加廢話註解。
